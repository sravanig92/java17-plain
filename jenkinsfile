node('jdk17-mvn3.9.1') {
    stage('git') {
    git branch: 'main', url: 'https://github.com/sravanig92/java17-plain.git'
    stage('build') {
        sh 'mvn clean package'
     }
     stage('archive') {
         archiv 'target/*.jar'
     }
}
}